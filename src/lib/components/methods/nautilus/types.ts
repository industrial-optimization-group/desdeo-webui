export enum PreferenceType {
  WEIGHT = "Weight",
  RANK = "Rank",
}

export enum IterationsType {
  AUTO = "Automatic",
  MANUAL = "Manual",
}

export enum AppState {
  IDLE = "IDLE",
  WORKING = "WORKING",
}
