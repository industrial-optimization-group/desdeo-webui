import { writable } from "svelte/store";

export const weightPreferences = writable([]);
export const inputWeights = writable([]);
export const rankPreferences = writable([]);
export const inputRanks = writable([]);
